<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="courtier_music.painter">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@drawable/ic_painter_launcher" android:theme="@style/AppTheme">
         <activity android:name=".UI.PainterStartActivity">
             <intent-filter>
                 <action android:name="android.intent.action.MAIN" />
                 <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
         </activity>
        <activity android:name=".UI.PainterMainActivity">
        </activity>
    </application>

</manifest>
